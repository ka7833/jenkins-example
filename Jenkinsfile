pipeline {
    agent any

    stages {
        stage('Checkout code') {
        steps {
            checkout scm
        }

        stage ('Deployment Stage') {
            steps {
                 echo 'Completed'
                }
            }
        }
    }
}
